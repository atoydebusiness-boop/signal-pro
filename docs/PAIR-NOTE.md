# Pares observados

Na sessão demo mostrada pelo usuário aparecem pelo menos `USD/JPY (OTC)` e `AUD/USD (OTC)`. Isso serve apenas como referência visual; a descoberta final deve vir dos dados de mercado validados, não de uma lista fixa.
