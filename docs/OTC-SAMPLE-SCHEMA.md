# Amostra OTC sanitizada

Campos úteis esperados para o parser: nome do ativo, timestamp e preço ou OHLC.

Exemplo conceitual: `{ "pair":"AUD/USD (OTC)", "timestamp":..., "price":... }`.

Não incluir campos de autenticação, sessão, conta ou ordem.
