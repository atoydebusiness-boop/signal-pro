# Banco — próxima etapa

Antes de integrar OTC ao histórico, adicionar uma coluna de origem/mercado em `signal_history` (por exemplo `market_source`) para persistir `forex` ou `quotex_otc`. Não inferir a origem pelo nome do par.
